# No test surface

This fixture deliberately declares no Makefile, package.json, pytest
configuration, compose file or `.skillbox/test.yaml`. Scoring it must ask for
input rather than invent a verdict about a suite it cannot see.
