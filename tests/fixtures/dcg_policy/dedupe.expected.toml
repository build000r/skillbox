# Auto-generated by skillbox runtime manager. Do not edit manually.
# skillbox-dcg-policy v1 - regenerate: python3 -m runtime_manager.dcg_policy render --output .dcg.toml
# fail_closed: unparseable hook input is DENIED, never allowed.

[general]
fail_closed = true

[packs]
enabled = ["core.git", "core.filesystem", "containers.docker", "database.postgresql"]

[overrides]
allow = ["docker compose down --volumes acme-dev"]
block = [{ pattern = "(?i)\\btmux\\s+capture-pane\\b", reason = "Direct `tmux capture-pane` polling is disabled. Use `ntm --ssh HOST --robot-is-working=SESSION --robot-terse` for status, `ntm --ssh HOST --robot-wait=SESSION --wait-until=idle --timeout=2h` for completion, or `ntm --ssh HOST --robot-tail=SESSION --lines=20` for output. The `vibing-with-ntm` skill is available for command selection and recovery. NTM pane indexes are not tmux pane ids; omit `--panes` unless you inspected the NTM mapping." }]
