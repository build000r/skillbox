Generated `.skill` bundles land here after `./scripts/03-skill-sync.sh` runs.

They come from the pinned sources in `workspace/default-skills.sources.yaml`
and the names listed in `workspace/default-skills.manifest`.
