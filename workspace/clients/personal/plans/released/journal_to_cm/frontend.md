# journal_to_cm — Frontend Spec

No frontend changes. This slice is entirely backend (Python runtime manager + MCP server).

The only user-visible change is the removal of the "Recent Activity" section from the generated CLAUDE.md context file, which is a backend context rendering change, not a frontend change.
